import type { ReactNode } from "react";

import "./dashboard-account-security.css";

export default function DashboardLayout({ children }: Readonly<{ children: ReactNode }>) {
  return <>{children}</>;
}
