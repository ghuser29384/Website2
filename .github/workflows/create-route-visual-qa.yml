name: Create route visual QA

on:
  pull_request:
    paths:
      - "public/moral-trade-create/**"
      - "src/app/create/**"
      - "src/app/trades/new/**"
      - "src/app/marketplace-ui.css"
      - "src/components/backing/**"
      - "src/components/create/**"
      - "src/components/marketplace/deal-receipt.tsx"
      - "src/lib/create-interface/**"
      - "src/lib/create-routes*"
      - "src/proxy.ts"
      - "src/proxy.test.ts"
      - "tests/create-route-workbench.spec.ts"
      - ".github/workflows/create-route-visual-qa.yml"

permissions:
  contents: read

jobs:
  create-route-qa:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint unified Create-route changes
        run: |
          npx eslint \
            src/proxy.ts \
            src/proxy.test.ts \
            src/app/create/page.tsx \
            src/app/trades/new/page.tsx \
            src/components/backing/backing-create-form.tsx \
            src/components/create/create-interface-frame.tsx \
            src/lib/create-routes.ts \
            src/lib/create-routes.test.ts \
            tests/create-route-workbench.spec.ts

      - name: Run Create routing unit tests
        run: node --import tsx --test src/proxy.test.ts src/lib/create-routes.test.ts

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Run unified Create interaction and visual QA
        env:
          CREATE_ROUTE_CAPTURE: "1"
        run: npm run test:e2e -- tests/create-route-workbench.spec.ts

      - name: Upload visual comparison artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: create-route-visual-qa
          path: test-results/create-route-visual
          if-no-files-found: error
          retention-days: 7
