name: Inspect PR 212 main-integration analysis

on:
  push:
    branches:
      - agent/pr212-main-integration-inspector-20260730
    paths:
      - .github/workflows/inspect-pr212-main-integration-analysis.yml
  workflow_dispatch:

permissions:
  actions: read
  contents: write

jobs:
  inspect:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      GH_TOKEN: ${{ github.token }}
      TARGET_WORKFLOW: analyze-pr212-main-integration.yml
      TARGET_BRANCH: agent/pr212-main-integration-ops-20260730
      TARGET_HEAD: 110a8de8334deb3f66516d07e138d2586c9d19a6
      INSPECTOR_BRANCH: agent/pr212-main-integration-inspector-20260730
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/pr212-main-integration-inspector-20260730
          fetch-depth: 0

      - name: Record exact analysis run
        shell: bash
        run: |
          set -euo pipefail
          sleep 30
          output=ops/pr212-main-integration/analysis-run
          mkdir -p "$output"
          gh run list \
            --repo "$GITHUB_REPOSITORY" \
            --workflow "$TARGET_WORKFLOW" \
            --branch "$TARGET_BRANCH" \
            --limit 30 \
            --json databaseId,headSha,status,conclusion,createdAt,url,displayTitle \
            > "$output/runs.json"
          run_id="$(jq -r --arg head "$TARGET_HEAD" '[.[] | select(.headSha == $head)] | sort_by(.createdAt) | reverse | .[0].databaseId // empty' "$output/runs.json")"
          if [[ -n "$run_id" ]]; then
            gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${run_id}" > "$output/run.json"
            gh api "repos/${GITHUB_REPOSITORY}/actions/runs/${run_id}/jobs?per_page=100" > "$output/jobs.json"
            gh run view "$run_id" --repo "$GITHUB_REPOSITORY" --log-failed > "$output/failed-log.txt" 2>&1 || true
          else
            printf '%s\n' '{}' > "$output/run.json"
            printf '%s\n' '{"jobs":[]}' > "$output/jobs.json"
            printf '%s\n' 'No exact analysis run found.' > "$output/failed-log.txt"
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$output"
          git commit -m "Inspect PR 212 main-integration analysis"
          git push origin "HEAD:refs/heads/${INSPECTOR_BRANCH}"
