name: Fix generated-feed composition counts

on:
  push:
    branches:
      - feature/bottleneck-atlas-feed-20260811
    paths:
      - .github/scripts/fix-feed-composition-count.py
      - .github/workflows/fix-feed-composition-count.yml

permissions:
  contents: write

jobs:
  patch-and-verify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: python .github/scripts/fix-feed-composition-count.py
      - run: npm ci
      - name: Focused tests
        run: >-
          node --import tsx --test
          src/lib/bottleneck-atlas.test.ts
          src/lib/opportunity-synthesis.test.ts
          src/bottleneck-atlas-feed-wiring.test.ts
      - name: Focused lint
        run: >-
          npx eslint
          public/moral-trade-live-now.js
          src/lib/bottleneck-atlas.ts
          src/lib/opportunity-synthesis.ts
          src/lib/bottleneck-atlas.test.ts
          src/lib/opportunity-synthesis.test.ts
          src/bottleneck-atlas-feed-wiring.test.ts
          src/app/bottleneck-atlas/page.tsx
          src/app/suggested-opportunities/[templateId]/page.tsx
          src/app/api/bottleneck-atlas/route.ts
          src/app/api/live-now/route.ts
          src/app/api/live-now/feedback/route.ts
          src/lib/site.ts
      - name: Typecheck
        run: npx tsc --noEmit --pretty false
      - name: Production build
        run: npm run build
      - name: Commit corrected feed counts
        run: |
          if git diff --quiet; then
            echo "No feed-count changes were produced." >&2
            exit 1
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f .github/scripts/fix-feed-composition-count.py
          rm -f .github/workflows/fix-feed-composition-count.yml
          git add -A
          git commit -m "Count all published feed opportunities separately"
          git push origin HEAD:${GITHUB_REF_NAME}
