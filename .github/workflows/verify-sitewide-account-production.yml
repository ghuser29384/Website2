name: Verify sitewide account identity on production

on:
  pull_request:
    paths:
      - ".github/workflows/verify-sitewide-account-production.yml"
      - "playwright.account-production.config.ts"
      - "tests/verify-production-account-identity.spec.ts"

permissions:
  contents: read

concurrency:
  group: production-account-identity-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  verify-production:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint production QA
        run: npx eslint tests/verify-production-account-identity.spec.ts playwright.account-production.config.ts

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Exercise production account identity
        run: npx playwright test --config=playwright.account-production.config.ts --reporter=line

      - name: Upload production screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: production-account-identity-clean-screenshots
          path: test-results/production-*-account.png
          if-no-files-found: error
          retention-days: 3
