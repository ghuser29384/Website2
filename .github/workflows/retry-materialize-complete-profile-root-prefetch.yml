name: Retry Complete Profile root-prefetch materialization

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/retry-materialize-complete-profile-root-prefetch.yml"

permissions:
  contents: write

concurrency:
  group: retry-materialize-complete-profile-root-prefetch-20260801
  cancel-in-progress: false

jobs:
  materialize:
    if: github.head_ref == 'fix/complete-profile-root-prefetch-20260801'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out cleanup branch
        uses: actions/checkout@v4
        with:
          ref: fix/complete-profile-root-prefetch-20260801
          fetch-depth: 0
          token: ${{ github.token }}

      - name: Materialize the cleanup and normalize EOF
        run: |
          set -euo pipefail
          python3 .github/scripts/materialize-complete-profile-root-prefetch.py
          python3 - <<'PY'
          from pathlib import Path
          path = Path("tests/complete-profile-review.spec.ts")
          path.write_text(path.read_text(encoding="utf-8").rstrip() + "\n", encoding="utf-8")
          PY
          rm .github/scripts/materialize-complete-profile-root-prefetch.py
          rm .github/workflows/materialize-complete-profile-root-prefetch.yml
          rm .github/workflows/materialize-complete-profile-root-prefetch-pr.yml
          rm .github/workflows/retry-materialize-complete-profile-root-prefetch.yml
          git diff --check
          node --check .github/scripts/complete-profile-production-canary.mjs

      - name: Commit and push final cleanup
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Stop Complete Profile home-link RSC prefetch"
          git push origin HEAD:fix/complete-profile-root-prefetch-20260801
