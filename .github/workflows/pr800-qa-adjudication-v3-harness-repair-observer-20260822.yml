name: PR800 QA adjudication v3 harness repair observer

on:
  push:
    branches:
      - ops/pr800-adjudication-observer-20260822
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  issues: write

concurrency:
  group: pr800-qa-adjudication-v3-harness-repair-observer
  cancel-in-progress: true

jobs:
  observe:
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    env:
      GH_TOKEN: ${{ github.token }}
      TARGET_WORKFLOW: pr800-qa-adjudication-v3-harness-repair-20260822.yml
      TARGET_BRANCH: ops/pr800-qa-adjudication-v3-harness-repair-20260822
      TARGET_HEAD: 867fc74be282c34f887e6845248a84f619f8de3e
    steps:
      - name: Observe exact harness repair run
        shell: bash
        run: |
          set -euo pipefail
          latest=''
          for attempt in $(seq 1 90); do
            response="$(gh api --method GET \
              "repos/$GITHUB_REPOSITORY/actions/workflows/$TARGET_WORKFLOW/runs" \
              -f branch="$TARGET_BRANCH" \
              -f per_page=30)"
            latest="$(jq -c \
              --arg sha "$TARGET_HEAD" \
              '[.workflow_runs[] | select(.head_sha == $sha)] | sort_by(.created_at) | reverse | .[0] // empty' \
              <<<"$response")"
            if [[ -n "$latest" ]]; then
              status="$(jq -r '.status' <<<"$latest")"
              if [[ "$status" = completed ]]; then
                break
              fi
            fi
            sleep 20
          done

          if [[ -z "$latest" ]]; then
            gh issue comment 709 --repo "$GITHUB_REPOSITORY" --body '<!-- pr800-qa-adjudication-v3-harness-repair-observer-v2 -->
          No exact harness-repair run was found for the repaired controller head.'
            exit 1
          fi

          run_id="$(jq -r '.id' <<<"$latest")"
          status="$(jq -r '.status' <<<"$latest")"
          conclusion="$(jq -r '.conclusion // "none"' <<<"$latest")"
          html_url="$(jq -r '.html_url' <<<"$latest")"
          body="<!-- pr800-qa-adjudication-v3-harness-repair-observer-v2 -->
          ## PR #800 QA adjudication harness-repair observer

          - run: \`$run_id\`
          - status: \`$status\`
          - conclusion: \`$conclusion\`
          - controller head: \`$TARGET_HEAD\`
          - run page: $html_url"
          gh issue comment 709 --repo "$GITHUB_REPOSITORY" --body "$body"
          test "$status" = completed
          test "$conclusion" = success
