name: Tighten Donation Redirect notification screen

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dormant:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow paused while the scheduled-redirect layout patch is replaced."
