name: PR 158 bilateral confirmation exact-head gate v2

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/pr158-bilateral-confirmation-exact-head-gate-v2.yml"

permissions:
  contents: read

concurrency:
  group: pr158-bilateral-confirmation-exact-head-gate-v2
  cancel-in-progress: true

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Superseded by PR 158 bilateral confirmation browser v3."
