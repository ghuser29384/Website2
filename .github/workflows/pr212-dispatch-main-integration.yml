name: Dispatch PR 212 Create iframe debug

on:
  push:
    branches:
      - agent/pr212-main-integration-ops-20260730
    paths:
      - .github/workflows/pr212-dispatch-main-integration.yml
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-24.04
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Dispatch exact Create-frame debug workflow
        run: gh workflow run pr212-create-frame-debug.yml --repo "$GITHUB_REPOSITORY" --ref agent/pr212-main-integration-ops-20260730
