[project]
name = "moral-trade-impact-v2"
version = "2.0.0"
description = "Repository-only, prior-driven Moral Trade causal resource forecast"
requires-python = "==3.12.13"
dependencies = ["numpy==2.3.5"]

[tool.unittest]
start-directory = "tests"

