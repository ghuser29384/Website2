# Release classification canary

This temporary, repository-only document exists only to verify the trusted `release-classification` status workflow after PR #428 was merged. It must not be merged into `main`.
